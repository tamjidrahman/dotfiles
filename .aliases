# Function to list directories in /src and cd into the selected one
repo() {
    ~/tmux-sessionizer
}

alias vim="nvim"
alias vi="nvim"

alias c="clear"

# ---- Eza (better ls) -----
alias ls="eza -a --color=always --long --git --no-filesize --icons=always --no-time --no-user --no-permissions"

# ---- Bat (better cat) -----
alias cat="bat"

