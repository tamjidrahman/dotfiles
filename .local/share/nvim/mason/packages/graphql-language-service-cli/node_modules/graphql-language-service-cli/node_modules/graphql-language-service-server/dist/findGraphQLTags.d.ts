import { Range } from 'graphql-language-service';
import type { Logger, NoopLogger } from './Logger';
import { parserMap } from './parsers';
declare type TagResult = {
    tag: string;
    template: string;
    range: Range;
};
export declare function findGraphQLTags(text: string, ext: keyof typeof parserMap, uri: string, logger: Logger | NoopLogger): Promise<TagResult[]>;
export {};
//# sourceMappingURL=findGraphQLTags.d.ts.map