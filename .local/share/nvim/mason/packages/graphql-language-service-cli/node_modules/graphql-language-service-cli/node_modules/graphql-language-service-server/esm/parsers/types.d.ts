import type { Range } from 'graphql-language-service';
import type { NoopLogger, Logger } from '../Logger';
export declare type RangeMapper = (range: Range) => Range;
export declare type SourceParserResult = null | {
    asts: any[];
    rangeMapper?: RangeMapper;
};
export declare type SourceParser = (text: string, uri: string, logger: Logger | NoopLogger) => Promise<SourceParserResult> | SourceParserResult;
//# sourceMappingURL=types.d.ts.map